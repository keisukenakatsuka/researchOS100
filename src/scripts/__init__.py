# src/scripts/__init__.py
# Standalone CLI scripts.  Each file is runnable via:
#   python -m src.scripts.<name>       (from project root)
#   python src/scripts/<name>.py       (with sys.path set to project root)
